apiVersion: v1
kind: Service
metadata:
  name: game-service
  namespace: 2048-game
  labels:
    app: 2048-game
spec:
  selector:
    app: 2048-game  
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      NodePort: 30080
  type: NodePort